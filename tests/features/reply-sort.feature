# language: zh-CN
功能: 评论按感谢数排序

  作为一个 V2EX 用户
  我希望能够将帖子评论按感谢数量排序
  以便我可以快速找到最受认可的回复

  场景: 解析有感谢数的评论
    假如 有一条评论包含感谢数 "6"
    当 解析该评论的感谢数时
    那么 应该返回 6

  场景: 解析无感谢数的评论
    假如 有一条评论没有感谢数
    当 解析该评论的感谢数时
    那么 应该返回 0

  场景: 单页评论按感谢数降序排序
    假如 有以下评论及感谢数:
      | 楼层 | 感谢数 |
      | 1    | 0      |
      | 2    | 6      |
      | 3    | 2      |
      | 4    | 0      |
      | 5    | 8      |
    当 按感谢数排序时
    那么 评论顺序应该为 "5,2,3,1,4"

  场景: 感谢数相同时保持原始顺序
    假如 有以下评论及感谢数:
      | 楼层 | 感谢数 |
      | 1    | 3      |
      | 2    | 3      |
      | 3    | 5      |
    当 按感谢数排序时
    那么 评论顺序应该为 "3,1,2"

  场景: 恢复默认排序
    假如 有以下评论及感谢数:
      | 楼层 | 感谢数 |
      | 1    | 0      |
      | 2    | 6      |
      | 3    | 2      |
    当 按感谢数排序时
    并且 恢复默认排序
    那么 评论顺序应该为 "1,2,3"

  场景: 登录详情页存在主题按钮区域时显示排序按钮
    假如 当前帖子详情页存在旧版主题按钮区域
    当 创建排序按钮时
    那么 应该在主题按钮区域显示 "感谢数排序" 按钮

  场景: 未登录详情页没有主题按钮区域时仍显示排序按钮
    假如 当前帖子详情页没有旧版主题按钮区域
    并且 回复列表头部显示 "39 条回复"
    当 创建排序按钮时
    那么 应该在回复列表头部显示 "感谢数排序" 按钮
