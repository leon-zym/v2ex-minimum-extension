---
description: BDD 测试开发规范 — 适用于测试文件和 .feature 文件
globs: tests/**/*.{ts,feature}
alwaysApply: false
---

# BDD 测试规范

## .feature 文件

- 使用中文 Gherkin 语法（`# language: zh-CN`）
- 放在 `tests/features/` 目录下
- 关键字：功能、场景、假如、当、那么、而且

## 步骤定义

- 放在 `tests/steps/` 目录下，文件名对应 feature 文件
- 使用 `vitest` 的 `describe/it` 包装 Gherkin 场景
- 步骤定义使用正则匹配 Gherkin 步骤文本

## 浏览器验证

- 单元/集成测试用 Vitest 运行
- E2E 验证用 chrome-devtools MCP 在真实浏览器中手动触发
- Safari 测试在功能完成后手动执行
