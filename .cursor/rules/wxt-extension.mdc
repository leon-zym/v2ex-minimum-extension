---
description: WXT 浏览器插件开发规范 — 适用于所有 TypeScript/React 源码文件
globs: src/**/*.{ts,tsx}
alwaysApply: false
---

# WXT 浏览器插件开发规范

## 入口文件

- Content Script、Background、Popup 等入口点放在 `src/entrypoints/`
- WXT 通过文件名自动识别入口类型，不要手动修改 manifest
- Content Script 使用 `defineContentScript`，Background 使用 `defineBackground`

## 导入路径

- 使用 `~/` 别名指向 `src/` 目录
- 示例：`import { featureManager } from '~/lib/feature-manager'`

## Feature 开发

- 新功能必须实现 `FeatureDefinition` 接口
- 功能模块放在 `src/features/<name>/` 目录下
- 功能元数据在 `src/features/registry.ts` 注册
- 功能运行时在 `src/features/index.ts` 注册
- setup 函数返回 cleanup 函数用于功能禁用时的清理

## 样式隔离

- 插件自有 UI：使用 `createShadowRootUi` + Tailwind CSS
- 修改网站样式：使用 `v2ex-min-` 前缀 CSS，通过 content script 注入
- 禁止使用 `!important`（除非覆盖目标已使用）
